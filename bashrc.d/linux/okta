okta-login() {
  if [[ -z "$1" ]]; then
    >&2 echo 'Usage: okta-login <profile>';
    return 1;
  fi
  export OKTA_PROFILE=$1
  export AWS_PROFILE=$1_source
  ~/.okta/login
}

export OKTA_PASSWORD_CMD='python -c "import keyring; print(keyring.get_password(\"okta\", \"sasha\"));"'
